<!DOCTYPE html>
<html lang="<%= ::I18n.locale %>">
  <head>
    <meta charset='<%= Rails.application.config.encoding %>' />
    <title>Мои квесты | Interactiff </title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%= csrf_meta_tags -%>
    <%= stylesheet_link_tag "http://interactiff.net/assets/application.css" %>
    <%= javascript_include_tag "application" %>
  </head>
  <body>
    <div class="allwrap">
      <div class="blue-wrap header">
        <div class="container">
        <div class="row-fluid">
          <div class="span3 logo"><a href="/"><img src="/assets/logo.png"></a></div>
          <div class="span6 center-me">
            <div class="navbar">
              <div class="navbar-inner">
                <ul class="nav">
                  <li><a href="">Тарифы</a></li>
                  <li><a href="">Помощь</a></li>
                  <li class="dropdown"><a href="" class="dropdown-toggle" data-toggle="dropdown"><i class="flags <%= ::I18n.locale %>"></i>Рус</a>
                    <ul class="dropdown-menu">
                      <li><%= link_to "" do %><i class="flags ru"></i>Русский<% end %></li>
                      <li><%= link_to "" do %><i class="flags en"></i>English<% end %></li>
                    </ul>
                  </li>
                </ul>
              </div>
            </div>
          </div>
          <div class="span3">
            <%=
              if current_user.is_a? Guest
                link_to "Вход или регистрация", new_user_registration_path, class: "btn pull-right"
              else
                link_to "Выйти", destroy_user_session_path, :method => :delete, class: "btn pull-right"
              end
            %>
          </div>
        </div>
        </div>
      </div>

      <div class="container">
        <%= yield %>
      </div>

      <div class="push"></div>
    </div>
    <div class="footer blue-wrap last-blue-wrap">
      <%= yield :footer %>
    </div>

  </body>
</html>
